using Unity.VisualScripting;
using UnityEngine;

public class spawnBoolean : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
   
   
    public bool alreadyOcuppied = false;
}
